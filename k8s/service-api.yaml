apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: ai-agents
  labels:
    app: api
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: api
