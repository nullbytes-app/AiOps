apiVersion: v1
kind: ResourceQuota
metadata:
  name: tenant-quota
  namespace: ai-agents-{{TENANT_ID}}
spec:
  hard:
    requests.cpu: "2"
    limits.cpu: "4"
    requests.memory: "4Gi"
    limits.memory: "8Gi"
    requests.storage: "20Gi"
    pods: "10"
